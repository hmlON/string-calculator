# frozen_string_literal: true
source 'https://rubygems.org'

# workaround for bundler errors
gem 'rainbow', '>= 2.1.0', '< 2.2.0'

group :test, :development do
  gem 'rspec'
end

group :development do
  gem 'guard-rspec'
  gem 'reek'
  gem 'rubocop'
end
